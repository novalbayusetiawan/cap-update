import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.novals.capupdate.example',
  appName: 'Cap Update Example',
  webDir: 'dist'
};

export default config;
