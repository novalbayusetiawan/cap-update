import XCTest
@testable import CapUpdatePlugin

final class CapUpdatePluginTests: XCTestCase {

    func testBundleIdDerivation() throws {
        // Placeholder test — add real tests as the plugin matures
        XCTAssertTrue(true)
    }
}
